# Runtime state lives under XDG dirs, never in the tree. These entries are a
# guard against a stray copy being staged -- the DSID cookie is a live session.
cookie
tunnel.pid
*.log
chrome/

__pycache__/
*.py[co]

CLAUDE.local.md
